name: journal_core_example
description: A test app for journal_core
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  journal_core:
    path: ../

flutter:
  uses-material-design: true

  assets:
    - assets/fonts/

  fonts:
    - family: Syllabi
      fonts:
        - asset: assets/fonts/syllabi.ttf
